<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <%= simple_form_for @list do |f| %>
          <%= f.input :name, placeholder: 'Insert a name' %>
          <%= f.input :description, placeholder: 'Insert a description' %>
          <%= f.input :photo, as: :file %>
          <div class="d-flex">
          <%= f.submit class: 'm-3 btn btn-primary' %>
        <% end %>
        <%= link_to "Back to lists", lists_path, class: "m-3 btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
</div>
